"""Integration-test placeholder for the Phase 3 video pipeline."""

import pytest


@pytest.mark.slow
def test_short_video_pipeline() -> None:
    """Record the intentionally deferred model/video integration contract."""

    pytest.skip("Phase 3 implements the short-video pipeline without CI model downloads")
